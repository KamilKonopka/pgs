import { InputAsteriskDirective } from './input-asterisk.directive';

describe('InputAsteriskDirective', () => {
  it('should create an instance', () => {
    const directive = new InputAsteriskDirective();
    expect(directive).toBeTruthy();
  });
});
